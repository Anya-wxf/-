Алгоритм имеет квадратичную сложность, если время выполнения пропорционально квадрату размера входных данных
Python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n-i-1):
            if arr[j] > arr[j+1]:
                arr[j], arr[j+1] = arr[j+1], arr[j]
//Внешний цикл выполняется n раз, а внутренний цикл — в среднем n/2 раз, что приводит к сложности (O(n^2)).
